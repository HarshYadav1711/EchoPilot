# EchoPilot

Local voice AI agent scaffold (Streamlit + faster-whisper + Ollama). **Implementation is not complete** — this repository contains structure, imports, and placeholders only.

## Setup

<!-- TODO: Python version, venv, `pip install -r requirements.txt`, Ollama install, model pull -->

## Architecture

<!-- TODO: Brief diagram or bullet list: app → core (STT, intent, router, memory) → tools → utils -->

## Safety

<!-- TODO: Sandbox path (`output/output/`), no cloud, future confirmation gates -->

## Demo

<!-- TODO: How to run `streamlit run app/streamlit_app.py`, sample audio, expected behavior -->
